plugins {
    id 'java'
    id 'application'
}

sourceSets {
    main {
        java {
            srcDirs = ['Code']
        }
        resources {
            srcDirs = ['Code']
            exclude '**/*.java'
        }
    }
}

repositories {
    mavenCentral()
    flatDir {
        dirs 'Code/ext'
    }
}

dependencies {
    implementation fileTree(dir: 'Code/ext', include: ['*.jar', '*.zip'])
    implementation 'org.apache.poi:poi:latest.release'
    implementation 'org.postgresql:postgresql:42.7.8'
}

application {
    mainClass = 'compucrash.CStart'
}

tasks.named('run') {
    args = ['Code/Compucrash.ini']
}
