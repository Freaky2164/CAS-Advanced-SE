spring:
  # Profil "dev": zusätzlich zum Schema (db/migration) wird Testdaten-Migration V5 (db/testdata)
  # eingespielt. Niemals in Produktion aktivieren (siehe docker-compose.yml vs. .override.yml)!
  flyway:
    locations: classpath:db/migration,classpath:db/testdata
